# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AirplaneMode' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AirplaneMode

  target 'AirplaneModeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AirplaneModeUITests' do
    inherit! :search_paths
    # Pods for testing
  end
	pod ‘RealmSwift’
end
